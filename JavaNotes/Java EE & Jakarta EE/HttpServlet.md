Обработчик HTTP запросов, наследуемся от него и переопределяем методы doGet(), doPost() и т. д., это позволяет нам писать свои обработчики на каждый из запросов.

Сам по себе наследуется от [[GenericServlet]].