package LeetCode;

public class Workout {
    public int maxSubArray(int[] nums) {
        int left = 0, right = 0;
        while (left != nums.length && right != nums.length) {
            int[] numsCp = nums.clone();
            if (left == right) {
                
            }
        }
    }
}
